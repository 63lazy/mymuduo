作为资深架构师，我必须提醒你 Acceptor 中两个硬核的细节，这是大厂面试最喜欢挖的坑：
accept4：在 Linux 下，推荐使用 accept4 而不是 accept。因为它可以在获取 fd 的同时，原子性地设置 SOCK_NONBLOCK 和 SOCK_CLOEXEC，避免了多线程竞态下的资源泄漏。
EMFILE 处理（文件描述符耗尽）：
如果服务器达到最大文件打开数，accept 会返回失败，但由于监听套接字依然可读（连接还在内核队列里），baseLoop 会陷入死循环（不断被唤醒但无法处理）。
工业级做法：预先准备一个空文件描述符（/dev/null）。一旦遇到 EMFILE，先关掉这个空文件，accept 掉这个多余的连接，然后立刻 close 它，再重新打开空文件。这能优雅地“礼貌回绝”超出限制的客户端。