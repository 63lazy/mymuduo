newConnection和connectionCallback_的区别:
newConnection是TcpServer设置给Acceptor用来建立连接并分发连接到subloop的
connectionCallback_是用户设置的连接成功之后的操作，比如输出日志提示连接成功了



为什么TcoServer对TcpConnection的连接和断开不直接写queueInLoop而是写runInLoop:
减少延迟：如果某些操作碰巧是在 BaseLoop 自己触发的（比如主线程因为某个定时器到期要主动踢掉连接），使用 runInLoop 可以原地立刻执行，不需要等待本轮 poll 结束再去处理 pendingFunctors_。
统一接口：开发者不需要在写代码时肉眼判断“我现在在哪个线程”，统一调用 runInLoop，由底层的 EventLoop 保证线程安全和执行时机。
比如当主线程决定销毁整个服务器时，它会遍历 connections_ 并对每个连接调用 runInLoop。由于此时主线程正在清理自己的 Map，这种“自清理”动作就是在主线程原地执行的。


在 Linux 网络编程中，accept 和 getsockname 这种函数，它们的长度参数 len 是必须初始化的。
为什么本地能过，云服务器会崩？
本地 (WSL)：WSL 在分配栈内存时，往往会默认清理为 0，或者刚好 addrlen 所在的位置残留了一个比较大的值，导致 getsockname 侥幸成功。
云服务器 (ECS)：原生 Linux 的栈内存完全是随机的。如果 addrlen 随机到了一个 0 或者 极小值：
getsockname 会执行失败，或者只拷贝了部分地址。
InetAddress localAddr(local) 拿到了乱码的内存。
当你调用 conn->localAddress().toIpPort() 准备打印日志时，inet_ntop 处理乱码地址会导致程序非法访问内存或直接崩溃。